angular.module('app.services.content', [])

.service('content', [
    '$q',
    '$http',
    function contentService($q, $http) {
        'use strict';

        var service = {

        };

        return service;
    }
])

;
